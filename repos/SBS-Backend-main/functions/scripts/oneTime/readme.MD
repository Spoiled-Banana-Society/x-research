`functions/scripts/oneTime`

These are scripts that intend to ever only be used once to fix something or put the data in a specifc state in an environment.  The could also be read only scripts that are used for testing certain things. 